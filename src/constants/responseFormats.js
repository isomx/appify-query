
export const PAYLOAD = 'payload';
export const RECORDS_RESULTS = 'results';
export const RECORDS = 'records';

const RESPONSE_FORMATS = {
  PAYLOAD,
  RECORDS_RESULTS,
  RECORDS
};

export default RESPONSE_FORMATS;